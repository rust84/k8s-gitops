createCRD: true
git:
  ssh:
    secretName: flux-git-deploy
chartsSyncInterval: "5m"
statusUpdateInterval: "5m"
prometheus:
  enabled: true
configureRepositories:
  enable: true
  repositories:
    - name: rust84
      url: https://rust84.github.io/helmcharts/
