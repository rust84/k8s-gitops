ingress:
  main:
    hosts:
      - host: radarr.$DOMAIN
        paths:
          - path: /
            pathType: Prefix
    tls:
      - hosts:
          - "radarr.$DOMAIN"
  api:
    hosts:
      - host: "radarr.$DOMAIN"
        paths:
          - path: /api
            pathType: Prefix
    tls:
      - hosts:
          - "radarr.$DOMAIN"
