ingress:
  main:
    hosts:
      - host: sonarr.$DOMAIN
        paths:
          - path: /
            pathType: Prefix
    tls:
      - hosts:
          - "sonarr.$DOMAIN"
  api:
    hosts:
      - host: "sonarr.$DOMAIN"
        paths:
          - path: /api
            pathType: Prefix
    tls:
      - hosts:
          - "sonarr.$DOMAIN"
