ingress:
  hosts:
    - host: "zigbee2mqtt.${DOMAIN}"
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - "zigbee2mqtt.${DOMAIN}"
mqtt:
  password: "${EMQX_MQTT_USER_PASSWORD}"