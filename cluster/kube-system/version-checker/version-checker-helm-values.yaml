apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: version-checker-helm-values
  namespace: kube-system
spec:
  keys:
    - values.yaml
  path: secrets/kube-system/version-checker/version-checker-helm-values
  type: Opaque