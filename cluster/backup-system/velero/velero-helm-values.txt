configuration:
  provider: aws
  backupStorageLocation:
    name: default
    bucket: velero
    config:
      region: us-east-1
      s3ForcePathStyle: true
      s3Url: https://synology.$DOMAIN:9000
      publicUrl: https://synology.$DOMAIN
  volumeSnapshotLocation:
    name: aws
    resticTimeout: 6h
    config:
      region: us-east-1
credentials:
  secretContents:
    cloud: |
      [default]
      aws_access_key_id = $MINIO_ACCESS_KEY
      aws_secret_access_key = $MINIO_SECRET_KEY
