apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: dex-helm-values
  namespace: security
spec:
  keys:
    - values.yaml
  path: secrets/security/dex/dex-helm-values
  type: Opaque
