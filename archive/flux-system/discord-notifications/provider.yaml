---
apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Provider
metadata:
  name: discord
  namespace: flux-system
spec:
  channel: flux
  username: flux-notifications
  type: discord
  secretRef:
    name: webhook-url

