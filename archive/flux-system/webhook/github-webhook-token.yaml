apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: github-webhook-token
  namespace: flux-system
spec:
  keys:
    - token
  path: secrets/flux-system/webhook/github-webhook-token
  type: Opaque
