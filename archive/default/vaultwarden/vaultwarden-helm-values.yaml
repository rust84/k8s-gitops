apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: vaultwarden-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/vaultwarden/vaultwarden-helm-values
  type: Opaque