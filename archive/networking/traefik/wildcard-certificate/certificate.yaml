---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: microserver-space
  namespace: networking
spec:
  secretName: microserver-space-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - microserver.space
    - "*.microserver.space"
