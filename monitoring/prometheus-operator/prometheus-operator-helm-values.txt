alertmanager:
  ingress:
    hosts: [prom-alert.k.$DOMAIN]
    tls:
    - hosts:
      - prom-alert.k.$DOMAIN
prometheus:
  ingress:
    hosts: [prom-server.k.$DOMAIN]
    tls:
    - hosts:
      - prom-server.k.$DOMAIN
grafana:
  ingress:
    hosts: [grafana.k.$DOMAIN]
    tls:
    - hosts:
      - grafana.k.$DOMAIN
  adminPassword: "$GRAFANA_PASSWORD"
  grafana.ini:
    server:
      root_url: https://grafana.${DOMAIN}
