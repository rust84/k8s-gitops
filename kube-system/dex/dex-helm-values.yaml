apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: dex-helm-values
  namespace: kube-system
spec:
  keys:
    - values.yaml
  path: secrets/kube-system/dex/dex-helm-values
  type: Opaque