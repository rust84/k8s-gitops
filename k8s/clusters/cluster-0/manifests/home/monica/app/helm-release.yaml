---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/helm.toolkit.fluxcd.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: monica
  namespace: home
spec:
  releaseName: monica
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.4.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      main:
        containers:
          main:
            image:
              repository: monica
              tag: 4.0.0-apache
            env:
              APP_ENV: production
              DEFAULT_MAX_STORAGE_SIZE: 4096
              APP_URL: https://monica.${SECRET_DOMAIN}
              MAIL_MAILER: smtp
              MAIL_HOST:
              MAIL_PORT: 25
              MAIL_ENCRYPTION: tls
<<<<<<< HEAD
            envFrom:
              - secretRef:
                  name: monica-secret
=======
            envFrom: monica-secret
>>>>>>> 9cafb1460 (feat(home): migrate monica to app-template)
    service:
      main:
        enabled: true
        ports:
          http:
            port: 80
    ingress:
      main:
        enabled: true
        ingressClassName: internal
        hosts:
          - host: &host "{{ .Release.Name }}.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
                service:
                  name: main
                  port: http
        tls:
          - secretName: "tls.{{ .Release.Name }}"
            hosts:
              - *host
    persistence:
      config:
        enabled: true
        existingClaim: monica-config
    resources:
      requests:
        cpu: 15m
        memory: 79M
      limits:
        memory: 284M
