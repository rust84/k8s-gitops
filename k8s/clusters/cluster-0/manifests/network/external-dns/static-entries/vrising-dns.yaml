---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: vrising-dns
  namespace: network
spec:
  endpoints:
    - dnsName: "vrising.${SECRET_DOMAIN}"
      recordTTL: 300
      recordType: CNAME
      targets:
        - ipv4.{$SECRET_DOMAIN}
