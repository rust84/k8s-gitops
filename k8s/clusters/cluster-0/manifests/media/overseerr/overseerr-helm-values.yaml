apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: overseerr-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/overseerr/overseerr-helm-values
  type: Opaque
