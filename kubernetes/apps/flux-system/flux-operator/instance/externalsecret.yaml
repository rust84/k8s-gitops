apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name flux-gitops
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: flux-gitops-secret
    template:
      data:
        githubAppID: '{{ .flux-operator_github_app_id }}'
        githubAppInstallationID: '{{ .flux-operator_github_app_installation_id }}'
        githubAppPrivateKey: '{{ .flux-operator_github_app_private_key }}'
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: flux-operator
    rewrite:
    - regexp:
        source: (.*)
        target: flux-operator_$1
