apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name radicale
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: *name
    creationPolicy: Owner
    template: {}
  dataFrom:
  - extract:
      key: radicale
