apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name authentik
  namespace: selfhosted
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: authentik-secret
    template:
      data:
        # App
        AUTHENTIK_EMAIL__HOST: '{{ .EMAIL__HOST }}'
        AUTHENTIK_EMAIL__USERNAME: '{{ .EMAIL__USERNAME }}'
        AUTHENTIK_EMAIL__PASSWORD: '{{ .EMAIL__PASSWORD }}'
        AUTHENTIK_EMAIL__FROM: '{{ .EMAIL__FROM }}'
        AUTHENTIK_POSTGRESQL__NAME: &dbName authentik
        AUTHENTIK_POSTGRESQL__HOST: &dbHost "postgres-rw.database.svc.cluster.local"
        AUTHENTIK_POSTGRESQL__USER: '{{ .POSTGRESQL__USER }}'
        AUTHENTIK_POSTGRESQL__PASSWORD: '{{ .POSTGRESQL__PASSWORD }}'
        AUTHENTIK_SECRET_KEY: '{{ .SECRET_KEY }}'
        INIT_POSTGRES_DBNAME: *dbName
        INIT_POSTGRES_HOST: *dbHost
        INIT_POSTGRES_USER: '{{ .POSTGRESQL__USER }}'
        INIT_POSTGRES_PASS: '{{ .POSTGRESQL__PASSWORD }}'
        INIT_POSTGRES_SUPER_PASS: '{{ .POSTGRES_SUPER_PASS }}'
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: authentik
  - extract:
      key: cloudnative-pg-superuser
