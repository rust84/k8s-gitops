---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memini
  namespace: ai
  labels:
    app.kubernetes.io/name: &name memini
    app.kubernetes.io/instance: *name
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
