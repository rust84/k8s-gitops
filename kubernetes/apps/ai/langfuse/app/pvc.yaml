---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: langfuse
  namespace: ai
  labels:
    app.kubernetes.io/name: &name langfuse
    app.kubernetes.io/instance: *name
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
