---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app ollama
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      ollama:
        replicas: 1
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          ollama:
            image:
              repository: ghcr.io/rust84/ollama-ov
              tag: latest@sha256:37365bfa8258cafc41076551a0a9b8b1041b2d41ae521e96dac38845f3239b72
            # command:
            #   - /bin/sh
            #   - -c
            #   - mkdir -p /llm/ollama && cd /llm/ollama && init-ollama && exec ./ollama serve
            securityContext:
              privileged: false
              capabilities:
                add:
                  - SYS_ADMIN
            env:
              OLLAMA_MODELS: &modelPath /models
              OLLAMA_HOST: 0.0.0.0
              GODEBUG: cgocheck=0
              no_proxy: localhost,127.0.0.1
            resources:
              requests:
                cpu: 200m
                gpu.intel.com/i915: 1
              limits:
                memory: 12Gi
                gpu.intel.com/i915: 1
        pod:
          affinity:
            podAntiAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - topologyKey: "kubernetes.io/hostname"
                  labelSelector:
                    matchExpressions:
                      - key: app.kubernetes.io/name
                        operator: In
                        values: [ *app ]
    service:
      ollama:
        controller: ollama
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: 10.20.69.125
          external-dns.alpha.kubernetes.io/hostname: ollama.${SECRET_DOMAIN}
        ports:
          http:
            port: 11434
    persistence:
      ollama:
        existingClaim: *app
        globalMounts:
          - path: *modelPath
            subPath: models
          - path: /root/.ollama
            subPath: config
      dri:
        type: hostPath
        hostPath: /dev/dri
