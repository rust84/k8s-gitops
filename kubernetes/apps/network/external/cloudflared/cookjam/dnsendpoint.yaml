---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/externaldns.k8s.io/dnsendpoint_v1alpha1.json
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: cloudflared-cookjam
spec:
  endpoints:
    - dnsName: "external.${SECRET_APP_DOMAIN}"
      recordType: CNAME
      targets: ["${SECRET_CLOUDFLARE_APP_TUNNEL_ID}.cfargotunnel.com"]
    # - dnsName: "external.${SECRET_APP_PRD_DOMAIN}"
    #   recordType: CNAME
    #   targets: ["${SECRET_CLOUDFLARE_APP_TUNNEL_ID}.cfargotunnel.com"]
