apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: vault-helm-values
  namespace: vault
spec:
  keys:
    - values.yaml
  path: secrets/vault/vault-test/vault-helm-values
  type: Opaque
