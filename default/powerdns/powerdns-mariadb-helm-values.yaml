apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: powerdns-mariadb-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/powerdns/powerdns-mariadb-helm-values
  type: Opaque