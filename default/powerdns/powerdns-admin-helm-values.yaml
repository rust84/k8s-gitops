apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: powerdns-admin-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/powerdns/powerdns-admin-helm-values
  type: Opaque