apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: powerdns-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/powerdns/powerdns-helm-values
  type: Opaque