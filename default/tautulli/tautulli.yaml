---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: tautulli
  namespace: default
  annotations:
    fluxcd.io/automated: "true"
    fluxcd.io/tag.chart-image: 'regexp:^(v2.[0-9]+\.[0-9]+-ls.*)$'
spec:
  releaseName: tautulli
  helmVersion: v3
  chart:
    repository: https://billimek.com/billimek-charts/
    name: tautulli
    version: 2.0.1
  values:
    image:
      repository: linuxserver/tautulli
      tag: v2.5.2-ls7
    puid: 1000
    pgid: 1000
    timezone: Europe/London
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: "nginx"
    strategyType: Recreate
    podAnnotations:
      backup.velero.io/backup-volumes: config
    persistence:
      config:
        enabled: true
        storageClass: rook-ceph-block
        accessMode: ReadWriteOnce
        size: 1Gi
    resources:
      limits:
        memory: 450Mi
      requests:
        cpu: 25m
        memory: 300Mi
    tolerations:
    - key: "arm"
      operator: "Exists"
  valueFileSecrets:
  - name: "tautulli-helm-values"
